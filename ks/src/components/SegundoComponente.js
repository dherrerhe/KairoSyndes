/**
 * Segundo componente de ejemplo para la aplicación.
 * 
 * Este es un componente funcional que se exporta como named export,
 * demostrando una alternativa al export default. Incluye funcionalidad
 * básica de renderizado.
 * 
 * @fileoverview Componente de ejemplo SegundoComponente
 * @author KairoSyndes
 * @version 1.0.0
 */

import React from 'react'

/**
 * Segundo componente funcional de ejemplo.
 * 
 * Este componente utiliza named export en lugar de default export.
 * Renderiza un div simple con texto de prueba, demostrando
 * una estructura alternativa de exportación en React.
 * 
 * @returns {JSX.Element} Elemento JSX del componente
 */
export const SegundoComponente = () => {
  return (
    <div>Probando segundo componente</div>
  )
}
